[tool:pytest]
minversion = 6.0
addopts = --cov=app --cov-report=term-missing --cov-report=html --cov-fail-under=85
testpaths =
    tests

[coverage:run]
branch = True
source =
    app

[coverage:report]
omit =
    */tests/*
    */__init__.py
    */migrations/*
